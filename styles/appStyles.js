import { StyleSheet } from "react-native";

export const appStyles = StyleSheet.create({
    app: {
        backgroundColor: 'black',
        flex: 1
    },
    text: {
        color: 'white'
    }
});